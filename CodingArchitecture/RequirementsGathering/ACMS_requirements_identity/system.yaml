fqsn: MetaArchitecture/ACMS_skill_generators/from_system.md_to_system.yaml
identity: ACMS Skill Artifact Generator for Mind Over Metadata LLC
agent_type: SkillArtifactGenerator
task_group: SystemConfiguration
workspace_key: MetaArchitecture
audit_log: 
- Component: system_configuration
- Artifact: system.yaml
- Cost entry written by PrincipalSystemArchitect after dispatch
- ADR-009 format, UPSTREAM_ID = PSA RUN_ID
parameters: 
- system.md
pipeline_steps: 
- Parse markdown sections
- Convert to YAML structure
- Validate key-value mapping
inputs: 
- system.md
outputs: 
- system.yaml
runtime_requirements: 
- Dispatched via fabric --pattern from_system.md_to_system.yaml
- Deployed to: ~/.config/fabric/patterns_custom/from_system.md_to_system.yaml/
- Temperature: 0
behavioral_contract: 
- Input is a system.md file piped via stdin
- Output is ONLY valid YAML — no preamble, no explanation, no markdown fences
- Extract all structured sections from system.md into YAML keys
- Preserve all values exactly as written — do not summarize or interpret
- Output is consumed by machines — parsability is paramount
- Missing sections are omitted entirely — never invent values
